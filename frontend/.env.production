# Used by `vite build` / Docker image — mocks must never ship in production
VITE_USE_MSW=false
# Same-origin API (backend serves /api on Cloud Run); do not use localhost in production
VITE_API_URL=/api
